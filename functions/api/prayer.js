import { json } from '../_utils.js';
export async function onRequestGet(context){const u=new URL(context.request.url),city=u.searchParams.get('city')||'Kabul',country=u.searchParams.get('country')||'Afghanistan',method=u.searchParams.get('method')||'2';const r=await fetch(`https://api.aladhan.com/v1/timingsByCity?city=${encodeURIComponent(city)}&country=${encodeURIComponent(country)}&method=${encodeURIComponent(method)}`);const d=await r.json();if(!r.ok||d.code!==200)return json({success:false,error:'اوقات نماز دریافت نشد.'},502);return json({success:true,date:d.data.date,timezone:d.data.meta.timezone,timings:d.data.timings});}
