import { json } from '../_utils.js';
export async function onRequestGet(context){const u=new URL(context.request.url),from=(u.searchParams.get('from')||'USD').toUpperCase(),to=(u.searchParams.get('to')||'AFN').toUpperCase(),amount=Number(u.searchParams.get('amount')||1);const r=await fetch(`https://api.frankfurter.app/latest?from=${encodeURIComponent(from)}&to=${encodeURIComponent(to)}`);const d=await r.json();if(!r.ok||!d.rates?.[to])return json({success:false,error:'نرخ ارز در دسترس نیست.'},502);return json({success:true,from,to,amount,result:amount*d.rates[to],rate:d.rates[to],date:d.date});}
